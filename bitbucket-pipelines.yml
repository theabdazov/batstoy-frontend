image: node:10.15.3

pipelines:
  default:
    - step:
        name: Build
        caches:
          - node
        script:
          - npm install
          - npm run build:ssr
          - npm run prerender
          - ls
          - ls dist
          - ls dist/server
